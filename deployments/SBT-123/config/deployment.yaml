apiVersion: v1
kind: Service
metadata:
  name: SBT-123
  labels:
    app: SBT-123
spec:
  ports:
    - name: http
      port: 8081
      targetPort: 80
  selector:
    app: SBT-123
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: SBT-123
  labels:
    app: SBT-123
spec:
  replicas: 1
  selector:
    matchLabels:
      app: SBT-123
  template:
    metadata:
      labels:
        app: SBT-123
    spec:
      containers:
        - name: SBT-123
          image: tutum/hello-world
